import { MentionLinkerPipe } from './mention-linker-pipe';

describe('MentionLinkerPipe', () => {
  it('create an instance', () => {
    const pipe = new MentionLinkerPipe();
    expect(pipe).toBeTruthy();
  });
});
